// Standard labels for urgency and importance
module.exports = {
  urgency: {
    TODAY: 'today', // tasks to be done today
    DEFAULT: null   // default is treated as 'someday'
  },
  importance: {
    HIGH: 'high',
    DEFAULT: null   // default is treated as 'low'
  }
};